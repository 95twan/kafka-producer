spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 10MB
      location: /Users/twan/Desktop/project-oj/upload
  config:
    import: file:./src/main/resources/config/secret.yml

server:
  port: 8080
